去掉不可达的状态

和非终态且不可到其他状态的状态

合并出方向路径相同 （到同一状态（集合）），并且同为（非）终态的状态

分割法
-----------------
一开始先将其划分为终态 h 和非终态

然后在其基础上不停分割

直至不可分

（注：自下而上的合并法存在疏漏
- $q_0 \xrightarrow{a} q_2$
- $q_1 \xrightarrow{a} q_3$
- $q_2 \xrightarrow{a} q_0$
- $q_3 \xrightarrow{a} q_1$
就不行）