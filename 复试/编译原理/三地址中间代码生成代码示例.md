X = a[i]
Y = b[j]
A[i] = y
B[j] = x

1. `LD R1, i` (获取 i 的值)
    
2. `MUL R1, R1, #4` (计算偏移量 $i * 4$)
    
3. `LD Rx, a(R1)` (加载 $a[i]$ 到 $Rx$)
    
4. `LD R2, j` (获取 j 的值)
    
5. `MUL R2, R2, #4` (计算偏移量 $j * 4$)
    
6. `LD Ry, b(R2)` (加载 $b[j]$ 到 $Ry$)
    
7. `ST a(R1), Ry` (将 $Ry$ 的值存入 $a[i]$)
    
8. `ST b(R2), Rx` (将 $Rx$ 的值存入 $b[j]$)

X = a[i]
Y = b[i]
Z = x * y

- `LD R1, i` (加载 i)
    
- `MUL R1, R1, #4` (计算偏移量 $i * 4$)
    
- `LD Rx, a(R1)` (加载 $a[i]$ 到 $Rx$)
    
- `LD Ry, b(R1)` (加载 $b[i]$ 到 $Ry$)
    
- `MUL Rz, Rx, Ry` (计算 $x * y$ 并存入 $Rz$)
    
- `ST z, Rz` (将结果存回内存变量 $z$)


IF x<\y goto L 1
Z=0
Goto L 2
L 2:Z=1
- **LD R1, x** (将变量 x 加载到寄存器 R1)
    
- **LD R2, y** (将变量 y 加载到寄存器 R2)
    
- **SUB R3, R1, R2** (计算 R3 = x - y)
    
- **BLTZ R3, L1** (如果 R3 < 0，即 x < y，则跳转到标签 L1)
    
- **LD Rz, #0** (如果不满足条件，执行 z = 0)
    
- **ST z, Rz** (将 0 存入内存 z)
    
- **BR L2** (无条件跳转到 L2，跳过 L1 部分)
    
- **L1: LD Rz, #1** (标签 L1 处，执行 z = 1)
    
- **ST z, Rz** (将 1 存入内存 z)